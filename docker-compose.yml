services:
  geth-triage:
    build: .
    restart: unless-stopped
    ports:
      - "8443:8443"
    volumes:
      - data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/geth-triage.db
      - LISTEN_ADDR=:8443

volumes:
  data:
